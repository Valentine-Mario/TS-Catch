import { Statement, ts } from "ts-morph";

const get_factory_method_and_labels = (
  input: Statement<ts.Statement>
): Statement<ts.Statement>[] => {
  return;
};

export default get_factory_method_and_labels;
